# Frontend Functionality (Phase 3 Prep)

This maps the Stitch screens to frontend behavior and required API contracts.

## 1) Reqstly Login Screen (`/login`)

Features:
- Email/password sign-in form.
- Show/hide password toggle.
- Passkey sign-in option.
- Microsoft Azure OAuth sign-in option.
- "Forgot password" link placeholder.
- Redirect to dashboard on success.

API contracts:
- route: `SUPABASE /auth/v1/token?grant_type=password`
- is protected: `no`
- purpose: Authenticate user with email/password.
- response: Supabase session payload with `access_token` on success.
- https method: `POST`
- other: Called via `supabase-js signInWithPassword`.

- route: `SUPABASE /auth/v1/authorize?provider=azure` (provider OAuth flow)
- is protected: `no`
- purpose: Start Microsoft Azure OAuth login flow.
- response: Redirect URL / OAuth session continuation.
- https method: `GET` (redirect flow via SDK)
- other: Called via `supabase-js signInWithOAuth` with provider `azure`.

- route: `SUPABASE /auth/v1/webauthn/passkey` (unofficial GoTrue route)
- is protected: `no`
- purpose: Start passkey authentication challenge for first-factor passkey login.
- response: `PublicKeyCredentialRequestOptions` payload.
- https method: `POST`
- other: Implemented in frontend; on deployments without this route GoTrue returns `404`.

- route: `SUPABASE /auth/v1/webauthn/passkey/verify` (unofficial GoTrue route)
- is protected: `no`
- purpose: Verify WebAuthn assertion generated by browser ceremony.
- response: Verification payload and/or session material.
- https method: `POST`
- other: Implemented in frontend passkey helper.

- route: `SUPABASE /auth/v1/webauthn/session` (unofficial GoTrue route)
- is protected: `no`
- purpose: Exchange passkey verification payload for session tokens when verify response does not include tokens directly.
- response: Access/refresh token payload.
- https method: `POST`
- other: Implemented as fallback in frontend passkey helper.

## 2) Login Error and Loading State (`/login`)

Features:
- Inline error banner for invalid credentials/config errors.
- Disabled inputs while authenticating.
- Submit button text changes to loading state.
- Covers failures from email/password, Microsoft Azure OAuth initialization, or passkey initiation (when enabled).

API contracts:
- route: `SUPABASE /auth/v1/token?grant_type=password`
- is protected: `no`
- purpose: Returns auth success/error used by UI state.
- response: Session on success, error payload on failure.
- https method: `POST`
- other: Same API as screen #1; this is a UI state variant.

## 3) Reqstly Dashboard Home (`/`)

Features:
- Sidebar/top-nav shell.
- Welcome panel and quick actions.
- Stat cards for open/in-progress/resolved counts.
- Recent activity list.

API contracts:
- route: `/api/v1/me`
- is protected: `yes`
- purpose: Fetch current user profile for shell/header.
- response: `ApiEnvelope<MeProfile>`
- https method: `GET`
- other: Used by app layout server load.

- route: `/api/v1/requests?status=<status>&page=1&limit=1`
- is protected: `yes`
- purpose: Fetch totals per status for dashboard cards.
- response: `ApiListEnvelope<SupportRequest>` with `meta.total`
- https method: `GET`
- other: Called for `open`, `in_progress`, `resolved`.

- route: `/api/v1/requests?page=1&limit=6&sort=-updated_at`
- is protected: `yes`
- purpose: Fetch recent request activity list.
- response: `ApiListEnvelope<SupportRequest>`
- https method: `GET`
- other: Used for recent activity panel.

## 4) Reqstly Dashboard Home:Mobile (`/`, responsive)

Features:
- Same dashboard data/features as desktop.
- Mobile nav drawer behavior and stacked layout.

API contracts:
- route: `/api/v1/me`
- is protected: `yes`
- purpose: Same as desktop dashboard shell.
- response: `ApiEnvelope<MeProfile>`
- https method: `GET`
- other: No mobile-specific API needed.

## 5) Reqstly Requests List View (`/requests`)

Features:
- Requests table.
- Filter controls (status/category/priority).
- Sort control.
- Pagination controls.
- Quick search (currently client-side on fetched page).

API contracts:
- route: `/api/v1/requests?page=<n>&limit=<n>&sort=<sort>&status=<status>&category=<category>&priority=<priority>`
- is protected: `yes`
- purpose: Fetch request list with server-side filtering/sort/pagination.
- response: `ApiListEnvelope<SupportRequest>`
- https method: `GET`
- other: `q` search is currently UI-only; backend search not yet wired.

- route: `/api/v1/meta/enums`
- is protected: `yes`
- purpose: Populate status/category/priority filter options.
- response: `ApiEnvelope<RequestEnums>`
- https method: `GET`
- other: Fallback values are present in UI if this fails.

- route: `/api/v1/requests?q=<term>` (planned)
- is protected: `yes`
- purpose: Server-side search by title/description/category/status.
- response: `ApiListEnvelope<SupportRequest>`
- https method: `GET`
- other: Missing currently; recommended for true large-list search.

## 6) Requests Empty State View (`/requests`, empty data state)

Features:
- Empty-state illustration/text.
- Primary CTA to create first request.

API contracts:
- route: `/api/v1/requests`
- is protected: `yes`
- purpose: Determine empty/non-empty list state.
- response: `ApiListEnvelope<SupportRequest>` with `meta.total === 0`
- https method: `GET`
- other: Same endpoint as screen #5.

## 7) Create New Request Form (`/requests/new`)

Features:
- Title, description, category, priority inputs.
- Context/help text.
- Submit and cancel actions.

API contracts:
- route: `/api/v1/meta/enums`
- is protected: `yes`
- purpose: Populate category/priority options.
- response: `ApiEnvelope<RequestEnums>`
- https method: `GET`
- other: Loaded before form render.

- route: `/api/v1/requests`
- is protected: `yes`
- purpose: Create a new request record.
- response: `ApiEnvelope<SupportRequest>` (new request)
- https method: `POST`
- other: Redirects to `/requests/{id}` on success.

## 8) Request Form Validation Errors (`/requests/new`, error state)

Features:
- Field-level validation messages.
- Form-level failure message.
- Retains previously entered values.

API contracts:
- route: `/api/v1/requests`
- is protected: `yes`
- purpose: Validate and reject invalid request payload.
- response: `ApiErrorEnvelope` with `error.details[]`
- https method: `POST`
- other: 422 handling implemented in form action.

## 9) Reqstly Request Detail View (`/requests/[id]`)

Features:
- Request metadata and editable form.
- Save changes action.
- Audit timeline panel.

API contracts:
- route: `/api/v1/requests/{id}`
- is protected: `yes`
- purpose: Fetch a single request.
- response: `ApiEnvelope<SupportRequest>`
- https method: `GET`
- other: Used to render detail + current values.

- route: `/api/v1/requests/{id}/audit`
- is protected: `yes`
- purpose: Fetch audit event timeline.
- response: `ApiListEnvelope<AuditLog>`
- https method: `GET`
- other: Sidebar timeline feed.

- route: `/api/v1/meta/enums`
- is protected: `yes`
- purpose: Populate editable enum fields.
- response: `ApiEnvelope<RequestEnums>`
- https method: `GET`
- other: Used for status/category/priority selects.

- route: `/api/v1/requests/{id}`
- is protected: `yes`
- purpose: Save edits to existing request.
- response: `ApiEnvelope<SupportRequest>`
- https method: `PATCH`
- other: Validation failures return `ApiErrorEnvelope`.

## 10) Delete Request Confirmation Modal (`/requests/[id]`, modal state)

Features:
- Destructive confirmation dialog.
- Cancel and delete actions.

API contracts:
- route: `/api/v1/requests/{id}`
- is protected: `yes`
- purpose: Permanently delete request.
- response: Empty/success envelope semantics (status-based in UI).
- https method: `DELETE`
- other: On success, UI redirects to `/requests`.

## 11) Reqstly Profile Overview Screen (`/profile`)

Features:
- Display name field (editable in UI).
- Read-only email and user ID.
- Add passkey action (WebAuthn MFA enrollment).
- Save changes button currently disabled.

API contracts:
- route: `/api/v1/me`
- is protected: `yes`
- purpose: Load profile identity data.
- response: `ApiEnvelope<MeProfile>`
- https method: `GET`
- other: Implemented now.

- route: `/api/v1/me` (planned)
- is protected: `yes`
- purpose: Update display name/profile metadata.
- response: `ApiEnvelope<MeProfile>`
- https method: `PATCH`
- other: Missing currently; required to enable Save Changes.

- route: `SUPABASE /auth/v1/factors`
- is protected: `yes`
- purpose: Enroll WebAuthn MFA factor for current authenticated user.
- response: Factor envelope including `id` for follow-up challenge.
- https method: `POST`
- other: Requires `Authorization: Bearer <access_token>` and `apikey` header. If disabled in GoTrue config, returns `mfa_webauthn_enroll_not_enabled`.

- route: `SUPABASE /auth/v1/factors/{factorId}/challenge`
- is protected: `yes`
- purpose: Request WebAuthn registration challenge (`type=create`).
- response: Challenge envelope with credential options.
- https method: `POST`
- other: Frontend sends `webauthn.rpId` and `webauthn.rpOrigins`.

- route: `SUPABASE /auth/v1/factors/{factorId}/verify`
- is protected: `yes`
- purpose: Verify WebAuthn registration response and rotate auth tokens.
- response: Access/refresh token payload with updated AAL context.
- https method: `POST`
- other: Frontend stores returned session via `supabase-js setSession`.

## 12) Sign Out Confirmation Modal (`/profile`, modal state)

Features:
- Sign-out confirmation dialog.
- Clears session and redirects to login.

API contracts:
- route: `SUPABASE /auth/v1/logout`
- is protected: `yes` (session required)
- purpose: Invalidate current Supabase session.
- response: Logout success/error from Supabase auth service.
- https method: `POST`
- other: Called via `supabase-js signOut`.

## 13) Reqstly Settings & Preferences (`/settings`)

Features:
- Notification toggles.
- Default page size setting.
- Save button currently disabled (frontend-only state).

API contracts:
- route: `/api/v1/preferences` (planned)
- is protected: `yes`
- purpose: Fetch user workspace preferences.
- response: `ApiEnvelope<{ email_digest:boolean; browser_alerts:boolean; default_page_size:number }>`
- https method: `GET`
- other: Missing currently.

- route: `/api/v1/preferences` (planned)
- is protected: `yes`
- purpose: Persist updated user preferences.
- response: `ApiEnvelope<{ ...updated preferences... }>`
- https method: `PATCH`
- other: Missing currently; required to enable Save Preferences.

## 14) Reqstly Sign Up Screen (`/signup`)

Features:
- Display name, email, password signup form.
- Error and informational states.
- Redirect on immediate session, else email-confirmation guidance.

API contracts:
- route: `SUPABASE /auth/v1/signup`
- is protected: `no`
- purpose: Register new user in Supabase auth.
- response: Session or pending-email-confirmation payload.
- https method: `POST`
- other: Called via `supabase-js signUp`.

## 15) Sign Up with Passkey Modal (`/signup`, modal state)

Features:
- Passkey onboarding modal requires `full name` and `email`.
- Loading state while passkey ceremony is being prepared (Stitch loading variant).
- Success state after passkey enrollment (Stitch success variant).
- Uses existing Supabase Auth MFA WebAuthn routes (no custom passkey tables).
- If email already exists, frontend starts email OTP sign-in flow and then links passkey after authenticated session.

API contracts:
- route: `SUPABASE /auth/v1/signup`
- is protected: `no`
- purpose: Create account when email is new (provisional password + metadata).
- response: Session payload (autoconfirm) or pending confirmation state.
- https method: `POST`
- other: Frontend uses metadata `display_name` from modal full-name field.

- route: `SUPABASE /auth/v1/otp` (via `supabase-js signInWithOtp`)
- is protected: `no`
- purpose: Existing-email branch to prove ownership and continue linking to same identity.
- response: OTP/magic-link dispatch acknowledgement.
- https method: `POST`
- other: Used when signup returns existing-user error.

- route: `SUPABASE /auth/v1/factors`
- is protected: `yes`
- purpose: Enroll `factor_type=webauthn` for current signed-in user.
- response: Factor payload with `id` and status.
- https method: `POST`
- other: Uses Supabase Auth infrastructure directly and returns `mfa_webauthn_enroll_not_enabled` when WebAuthn MFA is not enabled on Auth service.

- route: `SUPABASE /auth/v1/factors/{factorId}/challenge`
- is protected: `yes`
- purpose: Generate WebAuthn registration challenge for factor.
- response: WebAuthn challenge payload with `credential_options`.
- https method: `POST`
- other: `webauthn.rpId` and `webauthn.rpOrigins` required.

- route: `SUPABASE /auth/v1/factors/{factorId}/verify`
- is protected: `yes`
- purpose: Verify WebAuthn credential response and finalize factor enrollment.
- response: Access/refresh token payload.
- https method: `POST`
- other: Frontend applies returned tokens with `supabase-js setSession`.
