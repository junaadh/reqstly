# Database Configuration
DATABASE_URL=postgresql://reqstly:password@postgres:5432/reqstly
POSTGRES_USER=reqstly
POSTGRES_PASSWORD=password
POSTGRES_DB=reqstly

# Backend Configuration
BACKEND_PORT=3000
RUST_LOG=info

# Frontend Configuration
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3000

# JWT Configuration (generate with: openssl rand -base64 32)
JWT_SECRET=change-this-secret-in-production
JWT_EXPIRATION_HOURS=24

# Azure AD Configuration (for Phase 3 - Authentication)
AZURE_AD_CLIENT_ID=your-azure-ad-client-id
AZURE_AD_TENANT_ID=your-azure-ad-tenant-id
AZURE_AD_CLIENT_SECRET=your-azure-ad-client-secret

# Passkey/WebAuthn Configuration (for Phase 3 - Authentication)
RP_ID=localhost
ORIGIN=http://localhost:5173

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Docker Configuration
COMPOSE_PROJECT_NAME=reqstly
COMPOSE_FILE=docker-compose.yml

# Monitoring (Port mappings)
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
LOKI_PORT=3100

# Development
RUN_MODE=development
NODE_ENV=development
